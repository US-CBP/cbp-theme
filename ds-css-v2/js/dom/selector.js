/**
 * Engine for selecting DOM elements
 * @param {string} id - id of DOM element
 */

 class SelectorEngine {
  static findOne(id) {
    return document.getElementById(id);
  }

  static findAll()
}